<div class="container mt-4">
  <div class="row">
    <% @artists.each do |artist| %>
      <div class="col-sm-3">
        <div class="card text-center mt-4" style="width: 19rem;">
          <img
            src="<%= artist.image_url %>"
            class="card-img-top"
            alt="photo_<%= artist.name %>"
            style="height: 18rem;"
          />
          <div class="card-body">
            <h4 class="card-title"><%= artist.name %></h4>
            <%= link_to "Ver más", artist_path(artist), class: "btn btn-primary mt-2" %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
